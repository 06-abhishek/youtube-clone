Clone Repository = git clone https://github.com/06-abhishek/youtube-clone.git

setup .env file, by .env.example

start frontend ==> cd client ==> npm run dev
start backend ==> cd server ==> npm run dev

You can visit hosted website ==>
